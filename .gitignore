# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
server/data/
server/data-backups/
dist-server/
.vercel
.env*

# graphify: commit the graph itself (graph.json/report/manifest) so every
# agent/session can query it without rebuilding; the AST cache stays local
graphify-out/cache/
